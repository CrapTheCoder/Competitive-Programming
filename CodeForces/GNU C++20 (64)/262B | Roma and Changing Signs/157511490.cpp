bits/stdc++.h>
namespace std;

int long long

main() {
k; cin >> n >> k;

multiset<int> s;
int i = 0; i < n; i++) {
int x; cin >> x;
insert(x);


(true) {
auto x = *s.begin();
(x > 0 || k == 0) break;

erase(s.begin());
insert(-x);
--;


% 2) {
auto x = *s.begin();
erase(s.begin());
insert(-x);


<< accumulate(s.begin(), s.end(), 0) << endl;
