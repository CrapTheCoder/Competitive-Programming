bits/stdc++.h>
namespace std;

int long long
endl '\n'

MOD = 1e9 + 7;
INF = LLONG_MAX >> 1;

main() {
sync_with_stdio(false); cin.tie(NULL);

<int> s;

cards = 0;
int i = 1; i <= 30000; i++) {
cards += 2*i;
push_back(cards);
cards += i;


; cin >> tc;
(tc--) {
int n; cin >> n;

int c = 0;

while (n >= 2) {
c++;
auto it = prev(upper_bound(s.begin(), s.end(), n));
n -= *it;


cout << c << endl;

