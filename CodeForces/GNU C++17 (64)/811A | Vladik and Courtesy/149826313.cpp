bits/stdc++.h>
namespace std;

int long long
endl '\n'

MOD = 1e9 + 7;
INF = LLONG_MAX >> 1;

main() {
sync_with_stdio(false); cin.tie(NULL);

b; cin >> a >> b;

int cur = 1; ; cur++) {
cur % 2) ? (a -= cur) : (b -= cur);
(a < 0) { cout << "Vladik" << endl; break; }
(b < 0) { cout << "Valera" << endl; break; }

