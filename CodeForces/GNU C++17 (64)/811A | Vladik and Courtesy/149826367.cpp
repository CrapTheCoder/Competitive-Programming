bits/stdc++.h>
namespace std;

main() {
b; cin >> a >> b;

int cur = 1; ; cur++) {
cur % 2) ? (a -= cur) : (b -= cur);
(a < 0) { cout << "Vladik" << endl; break; }
(b < 0) { cout << "Valera" << endl; break; }

